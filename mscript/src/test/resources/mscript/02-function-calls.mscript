/*
 * TESTS:
 *  - simple (and not so simple) expressions, involving operators
 *  - function calls
 */

var1 = 1 + '3'
var1 = 1 * (2+ 3)/10% -200;

var2 = $g('var1', 10)
$g('a + b equals c');
$g('Hello $h('var1', 1)')

//

$web.click($h('ds1',$g('f3','Val')))

var2 = $web.getValue('f3','val') + 30
var1=$setVar('f1' ,$rand(5)) + '-OK string'

$h( '$g('3' * ($web._nativeFunc1()) * (2 + 1))-aa', 1, 2 )

$g( '$g('3' * $web._nativeFunc1()) * (2 + 1))-aa' )
